# CLAUDE.md

Public developer docs for cfe-api. This repo lives inside the `cfe-hub` fake monorepo (`~/Repositories/cfe-hub/`), next to `../cfe-api` — the API source.

**`llms.txt` here is a mirror.** The source of truth is `../cfe-api/static/llms.txt` (served live at the API's `/llms.txt`). Never edit `llms.txt` here directly: change the source, copy it verbatim, and add a `CHANGELOG.md` entry. Full contract in `../CLAUDE.md`.
