# CLAUDE.md

Public developer docs for cfe-api. This repo lives inside the `cfe-hub` fake monorepo, next to `../cfe-api` (the API source).

`llms.txt` here is a **mirror** of `../cfe-api/static/llms.txt` (the source of truth, served live at the API's `/llms.txt`). To change it: edit the source, copy it verbatim here, and add a `CHANGELOG.md` entry — full **mirror contract** in `../CLAUDE.md`.
